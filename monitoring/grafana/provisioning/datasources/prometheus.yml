apiVersion: 1

datasources:
  - name: Prometheus
    # The uid is referenced by every panel in the provisioned dashboards, so
    # it is pinned here rather than left to Grafana to generate.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
